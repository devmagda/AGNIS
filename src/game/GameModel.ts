import {Model} from "../modules/mvc/Model";

class GameModel extends Model {
    constructor() {
        super();

    }

    update(delta: number): void {
        super.update(delta);

    }
}

export {GameModel};